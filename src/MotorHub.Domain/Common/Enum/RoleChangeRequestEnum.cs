namespace MotorHub.Domain.Common.Enum
{
    public enum RoleRequestTypes
    {
        Pending,
        Approved,
        Rejected
    }

    public enum RoleChangeSortBy
    {
        CreatedAt,
        FirstName,
    }
    public enum RoleChangeSortOrder
    {
        Asc,
        Desc
    }
}
